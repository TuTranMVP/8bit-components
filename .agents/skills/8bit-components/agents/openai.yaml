interface:
  display_name: "8bit Components"
  short_description: "Repo-specific patterns and workflows for 8bit-components"
  default_prompt: "Use the 8bit-components repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true